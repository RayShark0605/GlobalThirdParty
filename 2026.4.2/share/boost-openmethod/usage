The package boost-openmethod is compatible with built-in CMake targets of FindBoost.cmake:

    find_package(Boost REQUIRED COMPONENTS openmethod)
    target_link_libraries(main PRIVATE Boost::openmethod)

or the generated cmake configs via:

    find_package( REQUIRED CONFIG)
    target_link_libraries(main PRIVATE Boost::openmethod)
